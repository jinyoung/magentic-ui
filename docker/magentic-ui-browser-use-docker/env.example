# ProcessGPT Browser Automation Server 환경변수 설정 예시
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요

# === 필수 환경변수 ===

# Supabase 설정 (ProcessGPT용)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# OpenAI API 키 (Browser-Use용)
OPENAI_API_KEY=your-openai-api-key-here

# === 선택적 환경변수 ===

# ProcessGPT 서버 설정
POLLING_INTERVAL=5                    # 폴링 간격 (초)
AGENT_ORCH=browser_automation_agent   # 에이전트 타입 식별자

# Browser-Use 설정
LLM_MODEL=gpt-4o-mini                # 사용할 LLM 모델
BROWSER_HEADLESS=false               # 헤드리스 모드 (true/false)
MAX_ACTIONS=50                       # 최대 액션 수
TASK_TIMEOUT=120                     # 태스크 타임아웃 (초)
SAVE_RECORDINGS=true                 # 브라우저 녹화 저장 (true/false)

# 시스템 설정
DISPLAY=:99                          # X11 디스플레이 (Docker용)

# === Supabase 설정 가이드 ===
# 1. https://supabase.com 에서 새 프로젝트 생성
# 2. Settings > API에서 Project URL과 anon public key 복사
# 3. 위의 SUPABASE_URL과 SUPABASE_ANON_KEY에 입력

# === OpenAI 설정 가이드 ===
# 1. https://platform.openai.com 에서 API 키 생성
# 2. 위의 OPENAI_API_KEY에 입력

# === 에이전트 타입 예시 ===
# browser_automation_agent  - 일반 브라우저 자동화
# web_scraping_agent        - 웹 스크래핑 전용
# testing_automation_agent  - 웹 테스트 자동화
# data_collection_agent     - 데이터 수집 전용
